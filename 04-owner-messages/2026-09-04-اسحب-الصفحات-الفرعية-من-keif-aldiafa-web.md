# رسالة المالك — 2026-09-04 — اسحب الصفحات الفرعية من مستودع «كيف الضيافة ويب»

## النص الحرفي
> الصفحات الفرعيه هل سويت لهن تصميم هل اعنت هيكلتهن

(ردّي: شرح الصفحات الخمس الموجودة في النموذج مع مونتاج.)

> الصفحات الفرعيه 
> مالك اسحب الباقي من المستودع الي اسمه كيف الضيافه ويب

> كمل

## فهمي
الموقع الفعلي (مستودع `keif-aldiafa-web` — Next.js) فيه صفحات فرعية غير موجودة في النموذج:
- `/locations` (فهرس المدن) + 8 صفحات مدن `/locations/<المدينة>`.
- 24 صفحة (خدمة × مدينة): `sababin-qahwa-*`، `qahwajiin-*`، `diyafa-munasabat-*` × 8 مدن.
- صفحة نية مستقلة `/mubashirin-qahwa-jeddah`.
- `/social` (الحسابات + باركود QR) و`/legal` (الحقوق).

المطلوب: نقلها كلها إلى النموذج الثابت بنفس هوية v6 (غلاف واحد من `index.html` + `build.py`)، مع تكييف النصوص لسياسات النموذج.

## القرارات (D55–D62)
- **D55 أسماء الملفات لاتينية مسطّحة** (لا مجلدات ولا عربية في الروابط): `locations.html`، `city-<slug>.html` ×8، `<service>-<city>.html` ×24 (نفس slugs الإنتاج: `sababin-qahwa-jeddah.html`…)، `mubashirin-qahwa-jeddah.html`، `social.html`، `legal.html`. المجموع 36 صفحة جديدة (42 صفحة إجمالاً).
- **D56 مولّد واحد** `local_page(rec)` في `build.py` يرسم صفحات (خدمة×مدينة) وصفحة النية بنفس الهيكل الإحدى عشري للإنتاج (هيرو + شارات، 3 أقسام متعاقبة نص/صورة مع «اسأل عن هذا»، 3 بطاقات خدمات، عدّة الضيافة (قصاصات الدلال والفناجين)، لماذا نحن ×6، معرض ×4، أسئلة، الأحياء، تواصل، صفحات أخرى). المحتوى في `data.py` (`local_content(service, city)`) مُترجم من `localContent.tsx` — مصدر واحد للحقيقة.
- **D57 تنقية الأرقام والأسعار**: حُذف/أُعيدت صياغة: «صبّاب لكل 40–60 ضيفاً»، «٤٨ ساعة»، «قبل أسبوع»، «200 ضيف»، «ثلاثة إلى أربعة صبّابين»، «الخمسين»، «عرض سعر مجاني»، «خلال دقائق». بقي فقط: +500 مناسبة / منذ 2016 / 7033069720. الحجز = «أرسل التاريخ والمكان وعدد الضيوف على واتساب ونعود إليك بالتشكيل المقترح».
- **D58 الصور**: الصفحات تعيد استخدام صور النموذج (`img/photos` + `img/full`) بمجمّعات لكل خدمة تتناوب حسب المدينة (لا صورة جديدة لكل مدينة). أُضيفت 5 لقطات كيف الضيافة ناقصة (`k-dallah-gold`، `k-dallah-silver`، `k-cups`، `k-tamr`، `k-spread`) + 6 قصاصات شفافة `img/cutouts/` (دلال وفناجين 640px) + باركود `img/brand/qr-keif-aldiafa.png` كما هو (PNG بلا ضغط ضائع — الضغط يُفشل المسح كما وثّق الإنتاج). لا صور حكومية.
- **D59 هيرو الصفحات**: مجمّع صغير من 7 صور هيرو مقصوصة (1:1 جوال / 3:1 ديسكتوب) عبر `images.py hero_variants` بدل 36 صورة مختلفة — حجم المستودع والأداء. `social.html` و`legal.html` بهيرو بلا صورة (تدرّج فقط).
- **D60 التنقّل**: أُضيف «المدن» إلى صف الهيدر (7 روابط) ويُميَّز عند أي صفحة مدينة/خدمة×مدينة (`aria-current="true"`؛ الصفحة نفسها `aria-current="page"`). التذييل: «المدن» → `locations.html`، «الحقوق القانونية» → `legal.html`، «حساباتنا» → `social.html`. شريط المدن في الرئيسية → صفحات المدن مباشرة. مسار التنقّل: الرئيسية › المدن › الصفحة.
- **D61 الروابط الداخلية** كما في الإنتاج: كل صفحة خدمة×مدينة تربط بالخدمتين الأخريين في نفس المدينة + نفس الخدمة في المدن الأخرى + صفحة النية (جدة فقط) + صفحة المدينة. صفحة المدينة تربط بخدماتها الثلاث + النية + بقية المدن.
- **D62 المراجعة والاختبار**: `review.py` يشمل الصفحات الجديدة (الصور المكرّرة تُحذف تلقائياً فتظهر فقط الصور الجديدة)؛ `audit_playwright.py` يفحص عيّنة 8 صفحات جديدة + الست القديمة على 390/1440؛ `check_static.py` كل الملفات.

## ملاحظات للمالك
- «جاهزية بنفس اليوم» و«خبرة +500 مناسبة» و«كل أحياء المدينة» = الشارات الثلاث في الهيرو كما بالإنتاج — بلا أرقام إضافية.
- أسماء الأحياء منقولة كما هي من الإنتاج (8 لكل مدينة، 12 لجدة في صفحة النية).


## تسوية التنفيذ (بعد البناء — 2026-09-04)
- **D56 (تعديل):** المحتوى المحلي يعيش في `build/local.py` (CITIES / LOCAL_SERVICES / POOL / HERO_POOL / local_content() / intent_content() / SOCIAL_NETS / LEGAL_SECTIONS) ويُستورد من `data.py`؛ و`build/local-data.json` نسخة مرجعية خام مُنقّاة من الاستخراج (tsx). المولّدات في `build.py`: `local_page()` (24 خدمة×مدينة + صفحة النية)، `build_city()` ×8، `build_locations()`, `build_social()`, `build_legal()`.
- **D57 (توسيع):** أُزيلت أيضًا وعود الوقت («حجز/ردّ خلال دقائق»، «ردّ فوري»، «طاقم فوري») من العناوين والأوصاف — لا نلتزم بزمن ردّ في النموذج. اسم الجهة الحكومية في أحياء ينبع أُزيل واستُبدل بـ«ينبع النخل».
- **الفحص:** 42 صفحة فرعية؛ check_static BAD 0 (كل *.html)؛ audit_playwright 0 أخطاء / 0 تمرير أفقي على 390 و1440 (عينة: locations, city-jeddah, qahwajiin-riyadh, mubashirin-qahwa-jeddah, social, legal)؛ لا روابط داخلية مكسورة.
