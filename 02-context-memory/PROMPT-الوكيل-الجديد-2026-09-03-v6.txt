أنت الوكيل الجديد لمشروع «كيف الضيافة» (keifaldiafa.com). أنا المالك MoTechSys — نادِني «يا غالي». الوكيل السابق أوقفته وأمرته بتوثيق كل شيء في المستودع لتُكمل أنت من نفس النقطة بلا فقدان سياق.

المستودع: https://github.com/MoTechSys/allpro (branch main) — استنسخه إلى /home/user/webapp.
نسخة النموذج للعرض: https://github.com/MoTechSys/khadamat (مُزامَنة مع آخر إصدار v6).

قبل أي عمل، اقرأ بهذا الترتيب ولا تتجاوز شيئاً:
1) 02-context-memory/HANDOFF-2026-09-03-v6.md  ← نقطة البداية: السياسات، الحالة الفعلية، النتائج، المهام المتبقية بالترتيب مع حلّها، الأوامر الجاهزة، القرارات المعلّقة عليّ.
2) 02-context-memory/STATE.md (آخر 3 قيود).
3) prototype-home/README.md §v6.
4) 01-keif-aldiafa/reports/25-v6-كل-الصفحات-وخط-الصور-والاختبارات-والقرارات.md
5) 04-owner-messages/ (آخر 3 ملفات — رسائلي حرفياً).
6) ثم للسياق العام: BOOTSTRAP.md, IDENTITY.md, USER.md, ENGINEERING.md, SOUL.md في 02-context-memory/.

بعد القراءة، أرسل لي أولاً ملخصاً موجزاً بالعربية (10 أسطر كحدّ أقصى) يثبت أنك فهمت: أين وصلنا، ما المتبقي، وما السياسات الحاكمة — ثم ابدأ التنفيذ مباشرة بلا انتظار قراري.

السياسات الثابتة (لا تُخالَف أبداً):
- لا Pull Requests: commit + push مباشر إلى main في allpro، ثم مزامنة khadamat.
- لا أسعار في أي صفحة.
- واتساب واحد 966508252134 (عرض 0508252134). الأرقام المسموحة فقط: +500 مناسبة، منذ 2016، السجل التجاري 7033069720.
- لا اسم أو شعار جهة حكومية بدون موافقة كتابية مني. شعارات الشركات مسموحة وتُوسَم «شركة».
- لا YouTube.
- الإنتاج keif-aldiafa-web/ (keif-v2) لا يُمسّ — مصدر صور فقط. لا نشر مستضاف إلا بطلبي.
- اختبر دائماً على 390 (جوال) و1440 (ديسكتوب): 0 أخطاء console، 0 تمرير أفقي، Lighthouse جوال ≥90، إتاحة 100.
- احفظ رسائلي حرفياً في 04-owner-messages/ وقيّد كل خطوة في STATE.md وارفعها.
- فسّر نيّتي لا حرف كلامي. لا تنتظر قراري في التفاصيل: حلّل ودقّق وقرّر بأفضل ما يمكن ووثّق القرار.
- الغلاف المشترك (CSS/هيدر/تذييل/Lightbox/JS) يُعدَّل في prototype-home/index.html فقط ثم python3 build.py — لا تحرّر الصفحات المولّدة يدوياً.

مهمتك الآن بالترتيب (التفاصيل والحلول في HANDOFF §3):
1. رفع أداء Lighthouse جوال لصفحات services (81) / offerings (71) / portfolio (69) إلى ≥90 بصورة هيرو متجاوبة لكل صفحة (picture + srcset + preload كما فُعل في v5.3 للرئيسية).
2. إصلاح أبعاد شعار الهيدر logo-emblem.webp → «أفضل الممارسات» 100 في كل الصفحات.
3. مراجعة بصرية كاملة الطول للصفحات الست على 390 و1440 وحفظ اللقطات في 01-keif-aldiafa/reports/shots/v6/.
4. تشغيل prototype-home/build/check_static.py و audit_playwright.py والتأكد من 0 مشاكل.
5. تحديث README §v6 + STATE.md + تقرير 25، commit + push إلى allpro، مزامنة khadamat.
6. تقرير عربي موجز لي مع رابط المعاينة (شغّل الخادم: cd prototype-home && python3 -m http.server 8787 ثم أعطني الرابط العام) والأسئلة التي تحتاج قراري (D31–D34, D39 في تقرير 25).

كمل بدقة وقوة، ووثّق كل شيء.
