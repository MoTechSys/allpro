# HANDOFF — 2026-09-05 — النموذج v6.9 «المشروع المتكامل» + صفحة الروابط والباركود — نقطة تسليم الوكيل الجديد

> **اقرأ هذا الملف أولًا** — كُتب بأمر المالك: «وثّق كل شيء. دقق، بحيث يجي أي وكيل بعدك أو بجلسة جديدة يقرأ كل شيء ويفهم كل شيء».
> يغطي كل ما حدث من `HANDOFF-2026-09-03-v6.md` إلى الآن (v6.3 → v6.9، القرارات D55–D102). ما قبله في ملف التسليم السابق.
> ترتيب القراءة بعده: §2 من هذا الملف (السياسات) → `STATE.md` (آخر 6 مداخل) → `prototype-home/README.md` → التقرير 29 و30 → آخر 5 ملفات في `04-owner-messages/`.

---

## 0. المالك وأسلوبه (لا يتغيّر)
- **MoTechSys** — يُنادَى «يا غالي». صاحب «كيف الضيافة» keifaldiafa.com (قهوجيين/صبّابين/ضيافة فاخرة — جدة + 7 مدن).
- يكتب بالعامية اليمنية/السعودية وباختزال وأخطاء إملائية («اشتي» = أريد، «سوي» = افعل، «اوك/اكي» = تمام). **فسّر النيّة لا الحرف.** كثيرًا ما يرفق لقطة شاشة — اللقطة هي المرجع.
- يفوّض بالكامل: «قرّر أنت ووثّق». يريد ردودًا **عربية موجزة** بعناوين، بروابط معاينة، وسؤالًا واحدًا فقط عند الضرورة.
- كل رسالة منه تُحفظ **حرفيًا** في `04-owner-messages/YYYY-MM-DD-<موضوع>.md` قبل التنفيذ، وتُلتزم بـcommit فوري.

## 1. المستودعات (تحقّق بأمر فعلي كل جلسة)
| المستودع | الدور | الحالة |
|---|---|---|
| `MoTechSys/allpro` main | **مستودع العمل + الذاكرة** — كل الكود (`prototype-home/`) والتوثيق | مستنسخ في `/home/user/webapp`. HEAD انظر `git log -1` |
| `MoTechSys/khadamat` main | **نسخة عرض** = محتوى `prototype-home/*` فقط (`.nojekyll`) | مستنسخ في `/tmp/khadamat` (بيئي — أعد الاستنساخ إن لم يوجد). **GitHub Pages غير مفعّل (404)** — يحتاج المالك: Settings → Pages → main / root |
| `MoTechSys/kaw` (keif-v2) | **الإنتاج Next.js** — للقراءة فقط (مرجع مكوّنات + صور) | `/tmp/kaw` بيئي؛ ⛔ لا يُمسّ |
| `MoTechSys/osoul-aldiafa-v2` | مشروع آخر للمالك (أصول الضيافة) — **مرجع تصميم** لصفحة /links فقط | `/tmp/osoul` بيئي |

أمر مزامنة khadamat (يُنفَّذ بعد كل push إلى allpro يمسّ `prototype-home/`):
```bash
cd /tmp/khadamat && git pull -q && find /tmp/khadamat -mindepth 1 -maxdepth 1 ! -name .git -exec rm -rf {} + \
 && cp -r /home/user/webapp/prototype-home/* /tmp/khadamat/ && rm -f review/*.zip && touch .nojekyll \
 && git add -A && git commit -qm "sync — <وصف>" && git push -q origin main
```

## 2. السياسات الثابتة (لا تُخالَف — التفصيل في MEMORY.md/BOOTSTRAP.md)
| # | السياسة |
|---|---|
| P1 | **لا Pull Requests** — commit + push مباشر إلى `main` في allpro، ثم مزامنة khadamat. |
| P2 | **لا أسعار** في أي صفحة. |
| P3 | واتساب واحد `966508252134` (يُعرض `0508252134`). الأرقام المسموحة فقط: **+500 مناسبة · منذ 2016 · س.ت 7033069720**. |
| P4 | **لا اسم/شعار جهة حكومية** في النصوص أو alt (الشهادات صور فقط). |
| P5 | لا YouTube (المالك: «ما عندي»). لا LinkedIn/Telegram كذلك (غير موجودة). |
| P6 | الإنتاج `kaw` لا يُمسّ؛ لا hosted deploy إلا بأمر صريح. |
| P7 | اختبار كل تغيير على **390 و1440**: 0 أخطاء console، 0 تمرير أفقي، Lighthouse جوال ≥90، a11y 100. SEO 60–63 **مقصود** (noindex على النموذج). |
| P8 | كل خطوة تُقيَّد في `STATE.md` بمدخل مؤرّخ + قرارات مرقّمة **D<n>**؛ **التالي الحر: D103**. |
| P9 | **D91**: لا أقواس حول أرقام في نص عربي (تنقلب bidi) — استخدم «·»: «عرض كل الشركاء · 23 جهة». |
| P10 | الشِل المشترك (رأس/فوتر/FAB/لايت بوكس/سكربت/CSS) يُعدَّل **في `prototype-home/index.html` فقط** ثم `python3 build.py` — لا تُعدَّل الصفحات المولَّدة يدويًا أبدًا. |

## 3. ما هو المشروع الآن — «المشروع المتكامل» v6.9
نموذج HTML ثابت كامل لموقع كيف الضيافة، بلغة تصميم واحدة، جاهز ليُستنسخ إلى Next.js (خريطة الاستنساخ في التقرير 29 §5).

### 3.1 الجرد — 43 ملف HTML في `prototype-home/`
| الفئة | العدد | الملفات | المولِّد في `build.py` |
|---|---|---|---|
| الرئيسية | 1 | `index.html` | **يدوي** — مصدر الحقيقة للشِل وCSS |
| عامة | 5 | services · offerings · portfolio · about · contact | `build_services/offerings/portfolio/about/contact()` عبر `shell()` |
| روابط مستقلة | 1 | `links.html` | `build_links()` — **بلا شِل** (وثيقة مستقلة 23KB) |
| بنية | 3 | locations · social · legal | `build_locations/social/legal()` |
| مدن | 8 | `city-{jeddah,makkah,madinah,riyadh,taif,dammam,abha,yanbu}` | `city_content()` → `master_page(kind='city')` |
| خدمة×مدينة | 24 | `{sababin-qahwa,qahwajiin,diyafa-munasabat}-{city}` | `local_content()` → `master_page(kind='svc')` |
| نيّة | 1 | `mubashirin-qahwa-jeddah` | `intent_content()` → `master_page(kind='intent')` |
| أرشيف | 1 | `hero-samples.html` | عيّنات خطوط (D98) — غير مربوط، لا يُحسب صفحة |

### 3.2 ترتيب أقسام الرئيسية (بعد D93/D94)
who → works (شريط حلقي) → partners (23) → staff → offerings → services → roles → packages → why → cities → faq → follow (socrow 5) → contact → **footer D94** (fgrid: العلامة+واتساب / الخدمات 7 / نخدم في 8 · دومات · شهادات كصور · س.ت).

### 3.3 صفحة master (svc / city / intent) — ترتيب ثابت
hero (3 شرائح crossfade) → works 8 صور → partners 6→23 داخل الصفحة → services → roles → packages (svc فقط) → why → faq → districts → contact → rel (روابط) → follow.

### 3.4 صفحتا التواصل والروابط (D99–D102 — آخر ما عُمل)
- **`contact.html`**: رأس أفقي مضغوط (`.lh-row`: الشعار يمينًا، يساره «تواصل معنا ✦ / كيف الضيافة / قهوجيين · صبّابين · ضيافة فاخرة») → **9 بطاقات قنوات** `.ch` بمقاس osoul (78px، أيقونة 3D 56px، سهم 32px) كل بطاقة بـ`--tint` لون منصّتها → نموذج «طلب عرض» (يفتح واتساب برسالة مُعدّة) → شريط المدن.
- **`links.html`**: نفس الرأس والبطاقات **فقط** + سطر «الموقع الرئيسي · © · س.ت» — بلا شارة/رأس/فوتر/FAB. الغرض: **باركود موحّد** يفتح الصفحة والعميل يختار القناة.
- **الأومنتريكس** (طلب المالك «مثل ساعة بن تن»): عند الضغط `.fire` → الأيقونة تتوهّج بلون المنصّة وتدور، حلقة `.ch-dial` تدور، حلقتان تتّسعان ×9، لمعة تجتاز، **وميض شاشة** `body.ch-flash::after` بلون المنصّة → الانتقال بعد 620ms (تبويب جديد للخارجي، نفس التبويب لـ tel:/mailto:). يحترم `prefers-reduced-motion`.
- **حاجز**: أي قناة رابطها ليس `https?://|tel:|mailto:` تُسقَط من البناء (مبدأ osoul `linksPlatforms.ts`).
- القنوات التسع: واتساب #25D366 · اتصال #E2C68E · إنستغرام #E1306C · تيك توك #69C9D0 · سناب #FFFC00 · إكس #D9D9D9 · فيسبوك #1877F2 · خرائط Google #34A853 · بريد #C5A059.
- **الأيقونات 3D** `img/links/icon-*.webp` (256² شفافة): whatsapp/instagram/tiktok/facebook من مستودع osoul (منظّفة)؛ tel/maps/mail/snapchat/x مولَّدة (nano-banana-pro بمرجع مجموعة osoul) — انظر D100.
- **الباركود** `python3 build/qr.py [URL]` → `qr/links-qr.svg` · `links-qr-1024.png` · `links-qr-card-1080.png`. الرابط الافتراضي **`https://keifaldiafa.com/links`** (الإنتاج المستقبلي؛ مسار Next.js `app/links` موجود). تحقّق فكّ التشفير بـOpenCV ✔. لباركود النموذج بعد تفعيل Pages: `python3 build/qr.py https://motechsys.github.io/khadamat/links.html`.

## 4. خريطة الكود `prototype-home/build/`
| ملف | الدور |
|---|---|
| `build.py` | المولِّد. يقتطع من `index.html`: `CSS, FONTS, HEADER, FOOTER, FAB, LB, SCRIPT, WA_SVG`. `STAMP='v6.9 · 2026-09-05'`. `shell()` غلاف الصفحات العامة؛ `phero()` هيرو داخلي؛ `sec_head()`؛ `contact_channels(src)` (رأس+بطاقات مشترك contact/links)؛ `build_links()` + `LINKS_CSS`؛ `build_contact()` + `CONTACT_JS/CONTACT_CSS`; `city_content()`; `master_page(rec)` + `MASTER_CSS/MASTER_HERO/HERO_SLIDES/MASTER_JS`; `build_local_all()`. |
| `data.py` | `WA_NUM, WA_DISPLAY, EMAIL, SOCIAL{}` (5 منصّات + maps)، `SERVICE_GROUPS`, `SERVICES` (15 خدمة)، يستورد `local.py`. |
| `local.py` | `CITIES` (8: slug/ar/region/districts/intro/lead/body/highlights/faqs)، `LOCAL_SERVICES` (3)، `INTENT_PAGES`، `POOL/HERO_POOL/CITY_HERO/CUTOUT_ITEMS`, `pick()`, `WHY_US()`, `local_content()`, `intent_content()`, `SOCIAL_NETS`, `LEGAL_SECTIONS`. |
| `images.py` + `images-manifest.json` | مقاسات الصور/هيرو (`HEROES`). |
| `review.py` | مراجعة بصرية/عدّ (يطبع «total N»). |
| `check_static.py` | يفحص كل `*.html`: روابط/صور/سياسات → يجب «BAD 0». |
| `audit_playwright.py` | تدقيق 20 صفحة × (390، 1440): أخطاء console، hscroll، صور مكسورة، لايت بوكس، فلاتر، نموذج. يحتاج خادمًا على 8787. اللقطات → `01-keif-aldiafa/reports/shots/v6/` (مستقل عن cwd منذ 2026-09-05). |
| `qr.py` | مولِّد الباركود (انظر §3.4). |
| `fonts.py` | تقليص الخطوط المحلية (`fonts/*.woff2`). |

## 5. دورة العمل القياسية (احفظها)
```bash
cd /home/user/webapp/prototype-home/build && python3 build.py && python3 review.py   # 42 صفحة، review total ~275
cd .. && python3 build/check_static.py                                                   # BAD 0
# خادمان محليان (إن لم يكونا يعملان):  python3 -m http.server 8787  (في prototype-home)  · gzserve على 8788 لـ Lighthouse
python3 build/audit_playwright.py           # 20 صفحة × 2 — كل errors []
PORT=8788 /tmp/lh/run.sh contact.html mobile   # سكربت Lighthouse بيئي (/tmp/lh) — إن غاب: npm i lighthouse في /tmp/lh وأعد كتابته (انظر STATE 2026-09-04)
git add -A && git commit -m "..." && git push origin main     # ثم مزامنة khadamat (§1)
```
Playwright جاهز في البيئة (`python3 -c "from playwright.async_api import async_playwright"`). إن غاب: `pip install playwright && playwright install chromium`.

## 6. نتائج الفحص الأخيرة (2026-09-05، HEAD هذا التسليم)
- `build` 42 · `review` 275 · `check_static` **BAD 0**.
- `audit_playwright` **40/40** (20 صفحة × جوال/سطح) errors [] · hscroll 0 · صور مكسورة 0 · LB يعمل · فلاتر portfolio · نموذج contact يولّد رسالة واتساب.
- Lighthouse جوال: index **93**/100/100/63 · contact 98 · links **99** · city-jeddah 97 · qahwajiin-jeddah 98 · services 99 — كل a11y 100، bp 100. (index 93 بسبب DOM ≈930 عنصرًا من استنساخ الأشرطة الحلقية D92 + فيديو الهيرو — مقبول ومعروف.)

## 7. سجل القرارات D55–D102 (مختصر — التفصيل في STATE.md)
| D | القرار |
|---|---|
| D55–D62 | v6.3: 36 صفحة محلية مولَّدة (مدن/خدمة×مدينة/نيّة/social/legal). |
| D63–D78 | v6.4–v6.5: صفحة master (qahwajiin-jeddah) 15 قسمًا، socrow، أيقونات Simple Icons. |
| D79–D90 | v6.6: أشرطة حلقية بطيئة، لايت بوكس D80، شركاء 23، نصوص الأدوار من مصادر سعودية D87، صف تواصل بألوان العلامات D90. |
| D91–D93 | v6.7: لا أقواس حول الأرقام؛ الشريط دائرة في الاتجاهين؛ الرئيسية تحوي أقسام master الأربعة. |
| **D94** | فوتر مُهيكل (fgrid + دومات + شهادات + س.ت) — مصدر واحد في index. |
| **D95–D97** | 24 + 8 + 1 صفحة كلها عبر `master_page` (kind svc/city/intent) — حُذف التخطيط القديم نهائيًا. |
| **D98** | `hero-samples.html` أرشيف فقط. |
| **D99** | contact على نمط صفحة روابط + أومنتريكس. |
| **D100** | بطاقات بمقاس osoul 78px + أيقونات 3D + سطح واقعي 4 طبقات + hover للمؤشّر فقط. |
| **D101** | `links.html` مستقلة + `qr.py` + `contact_channels(src)` مشترك. |
| **D102** | رأس أفقي مضغوط (الشعار يمين/النصوص يسار) — 8/9 بطاقات فوق الطيّة على 390×844. |

## 8. بانتظار المالك / المهام المفتوحة
1. **اعتماد v6.9** كنسخة «المشروع المتكامل» → بعدها **الاستنساخ إلى Next.js** وفق التقرير 29 §5 (ابدأ بـ `Footer.tsx`، ثم `LocalServicePage` بخاصية `kind`، ثم `/links`، ثم الرئيسية).
2. **تفعيل GitHub Pages لـ khadamat** (المالك) → إصدار باركود النموذج.
3. اقتراح مفتوح: تقليص فجوة البطاقات 11→9px لتدخل التاسعة (البريد) الشاشة الأولى.
4. أقدم: تسجيلات أحمد/جدول التسمية؛ «جناح فندق»؛ +24 شريك؛ D31–D34، D39.
5. **لا تعمل على أي شيء لم يطلبه المالك أو لم يُدرَج هنا** إلا بقرار موثَّق D<n>.

## 9. أشياء بيئية تُعاد إن ضاعت (لا تُفترض)
- `/tmp/khadamat`, `/tmp/kaw`, `/tmp/osoul` — استنساخات؛ `/tmp/lh/run.sh` — Lighthouse؛ `/tmp/cx/test.py` — اختبار contact/links السريع؛ `/tmp/shots/` — لقطات عمل.
- الخادمان 8787 (http.server في `prototype-home/`) و8788 (gzip) — أعد تشغيلهما بـ `run_in_background`.
- رابط المعاينة يتغيّر مع كل sandbox — استخدم `GetServiceUrl` للمنفذ 8787.
