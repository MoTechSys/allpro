# BOOTSTRAP.md — بروتوكول استعادة السياق (يقرؤه أي وكيل جديد أولاً)

> **الغرض:** أي جلسة/وكيل جديد يستعيد كامل السياق من هذا الملف بترتيب القراءة أدناه — بلا اعتماد على ذاكرة محادثة سابقة. هذا هو خط الدفاع الأول ضد الهلوسة وفقدان السياق في البرمجة طويلة المدى.

## ⚠️ قاعدة الذهب
**لا تثق بأي معلومة بيئية من جلسة سابقة** (مسارات، مفاتيح، أدوات) — البيئات تتغير بين الوكلاء. الثابت الوحيد: المستودعات الثلاثة على GitHub وقواعد المالك. كل شيء بيئي يُعاد التحقق منه بأمر فعلي قبل الاستخدام.

## ترتيب القراءة الإلزامي لأي جلسة جديدة
1. هذا الملف (BOOTSTRAP.md)
2. `HANDOFF-2026-09-01.md` — لقطة التسليم الكاملة (إن كنت وكيلاً جديداً تبدأ من الصفر)
3. `STATE.md` — الحالة اللحظية: أين نحن الآن بالضبط، وما التالي
4. `MEMORY.md` — قيود المالك + الحقائق الدائمة المثبتة
5. `01-keif-aldiafa/reports/10-الخطة-المتكاملة-الشاملة.md` — الخطة الحاكمة + `11-التوليفة-الكبرى` — مصفوفة النوايا وترتيب الموجات
6. آخر ملف في `04-owner-messages/` — آخر توجيه من المالك
7. `05-incoming-research/inbox/` — هل يوجد وارد جديد لم يُحلل؟
8. آخر تقرير سجل تنفيذ في `reports/` (حالياً 17) — كيف نُفّذت آخر صفحة (النمط المعتمد)

## المستودعات الثلاثة (ثابتة)
| المستودع | الدور | القاعدة |
|---|---|---|
| MoTechSys/keif-aldiafa-web | ⭐ العمل | كل الكود هنا فقط |
| MoTechSys/keif-v2 | 🔴 الإنتاج (Vercel→keifaldiafa.com) | ⛔ لا يُلمس إلا بأمر نشر صريح من المالك |
| MoTechSys/allpro | 📚 التوثيق والذاكرة | كل قرار/تقرير/رسالة مالك يُرفع هنا فوراً |

## 🔴 السياسات الدائمة من المالك (محسومة — لا تُناقش، لا تُنسى)
1. **لا Pull Requests أبداً** — الوكيل مدير المشروع: يدمج مباشرة في `main` بعد اجتياز بوابة الجودة كاملة. (PR #2 كان آخر PR في تاريخ المشروع — مغلق merged=True)
2. **قبل أي دمج كبير:** فرع احتياطي من main باسم `old-version-YYYY-MM-DD` يُدفع أولاً
3. **لا تسعير إطلاقاً** — لا أرقام أسعار في أي صفحة؛ التوجيه دائماً واتساب +966508252134
4. **يوتيوب = لا يوجد** (المالك: «ماعندي») — لا يُضاف أبداً في sameAs أو أي مكان
5. **لا أرقام غير موثقة** — أي ادعاء رقمي يمر عبر `data/proof.json` بـ `verified_by_owner:true`. الصيغة الوحيدة المعتمدة: «+500 مناسبة نفّذها فريقنا بأنفسنا في جدة والمنطقة الغربية». الادعاءات 200/100%/10 سنوات ما زالت **غير موثقة** — لا تُستخدم
6. **«الدقة والقوة أهم من الأرصدة»** — لا اختصار للتحقق الحي توفيراً للتكلفة
7. **التدقيق المضاعف:** «حتى ولو كان موثق تتأكد بدقة» — أعد التحقق حياً (curl/مصدر رسمي) وقت التنفيذ حتى للحقائق الموثقة هنا
8. **النشر للإنتاج (keif-v2) بأمر صريح منفصل فقط** — إنجاز العمل في مستودع العمل لا يعني النشر

## قواعد مضادة للهلوسة (ملزمة لكل وكيل)
1. **كل ادعاء بدليل:** ملف:سطر، أو مخرجات أمر، أو بيانات GSC/Ads، أو مصدر رسمي — وإلا لا يُكتب
2. **لا استنتاج بلا قياس:** الأداء يُقاس بأداة (Lighthouse/curl)، لا يُخمَّن
3. **التحقق قبل البناء:** أي تقرير خارجي يُتحقق من ادعاءاته (سابقة: باحث ادّعى عدم وجود GBP خطأً؛ محلل خارجي ادّعى غياب schema موجودة)
4. **اكتب فوراً، لا تؤجل:** أي حقيقة جديدة/قرار مالك → يوثق في allpro في نفس الجلسة (الذاكرة خارج المحادثة لا داخلها)
5. **حدّث STATE.md قبل نهاية كل جلسة عمل** — هو نقطة التسليم بين الجلسات
6. **عند الشك اسأل المالك** — سؤال واحد أرخص من هلوسة واحدة

## قيود المالك المقدسة (نسخة سريعة — التفصيل في MEMORY.md)
لا تسعير إطلاقاً · لا أرقام غير موثقة (proof.json) · لا ترقيع — جذري فقط · Next.js 14 App Router + SSG · عربي للمحتوى لاتيني للكود · 99% جوال — الهاتف هو الحاكم · صفحة واحدة لكل نية مستقلة — **ممنوع doorway** (لا استنساخ مدن) · كل قرار موثق (ماذا/لماذا/الدليل)

## ⚙️ بوابة الجودة الإلزامية قبل كل دمج (الأوامر الحرفية، بهذا الترتيب)
```bash
cd /home/user/webapp   # أو مسار مستودع العمل في بيئتك
npx tsc --noEmit                  # يجب exit 0
npx next lint                     # يجب: ✔ No ESLint warnings or errors
npx next build                    # يجب أن ينجح كاملاً
npm run guard                     # 3 حراس — خطوط الأساس الحالية:
#   seo-guard:        121 مخالفة قديمة (baseline 156) — ممنوع أي مخالفة جديدة
#   similarity-check: 118 زوجاً (baseline)             — ممنوع أي زوج جديد ≥60%
#   assets-guard:     31 (baseline)                    — ثابت
```
ثم **فحص يدوي للـ HTML الناتج** في `.next/server/app/*.html`: العنوان ≤60 حرفاً مع اللاحقة « | كيف الضيافة» · الوصف ≤158 · canonical صحيح · h1 واحد فقط · **FAQPage JSON-LD = صفر** (متوقفة رسمياً 2026-05-07) · الكلمة المفتاحية في أول 100 كلمة · الصفحة في sitemap · الروابط الداخلية المتبادلة موجودة.

## 🧩 بروتوكول إضافة صفحة نية جديدة (النمط المعتمد — مثال حي: تقرير 17)
1. **إعادة تحقق حي قبل الكتابة:** curl على الإنتاج (الصفحة 404 فعلاً؟) + مسح SERP حي للمنافسين
2. **سجّل الصفحة في `INTENT_PAGES`** داخل `src/lib/localPages.ts` — هذا المصدر الوحيد يغذي تلقائياً: sitemap + بطاقة في صفحة المدينة + روابط داخلية في صفحات خدمات المدينة نفسها (عبر `otherCitiesLinks` في `localContent.tsx`)
3. **محتوى يدوي فريد 100%** بمفردات التصميم الفاخر الموجودة (`LocalServicePage.tsx` هو قاموس الأصناف: ls-* / lx-*؛ الصور من `localImagery.ts`)
4. **الأسئلة الشائعة مرئية** بـ `<details>` — بلا FAQPage JSON-LD؛ السكيمات المسموحة: Breadcrumb + Service + WebPage
5. **احسب أطوال العنوان/الوصف برمجياً قبل الكتابة** (title ≤60 مع اللاحقة، description ≤158)
6. **قِس التشابه برمجياً** ضد صفحات المدينة نفسها بدوال `scripts/similarity-check.mjs` (extractVisibleText/neutralize/ratio) — الحد الأقصى 60%، الهدف <45% (آخر صفحة حققت 36–40%)
7. بوابة الجودة كاملة → دمج مباشر في main → توثيق تقرير سجل تنفيذ مرقم في allpro + تحديث STATE.md

## 🔧 ملاحظات بيئة (تُعاد تجربتها في كل بيئة جديدة)
- **خدعة توكن GitHub:** التوكن قد ينتهي منتصف الجلسة — عند فشل push بخطأ مصادقة: أعد تهيئة بيئة GitHub (في بيئة Genspark: أداة `setup_github_environment`) ثم أعد المحاولة
- **مستنسخات المراجع لا تنتقل بين البيئات:** `royal-coffee-docs` و `osoul-aldiafa-v2` (مستودعا مراجع للمالك) كانت في `/tmp/repos/` ببيئة سابقة — إن احتجتها أعد استنساخها من GitHub
- **PSI API** كان متجاوزاً للحصة اليومية (2026-09-01) — أعد المحاولة عند الحاجة لقياس LCP الرسمي
- الوثيقة الذهبية لقواعد السيو: `osoul-aldiafa-v2/docs/99-archive/plan-ar/07-قواعد-السيو-الملزمة.md`

_آخر تحديث: 2026-09-01 (جلسة التسليم)_
