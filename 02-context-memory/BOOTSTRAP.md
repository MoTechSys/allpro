# BOOTSTRAP.md — بروتوكول استعادة السياق (يقرؤه أي وكيل جديد أولاً)

> **الغرض:** أي جلسة/وكيل جديد يستعيد كامل السياق من هذا الملف بترتيب القراءة أدناه — بلا اعتماد على ذاكرة محادثة سابقة. هذا هو خط الدفاع الأول ضد الهلوسة وفقدان السياق في البرمجة طويلة المدى.

## ⚠️ قاعدة الذهب
**لا تثق بأي معلومة بيئية من جلسة سابقة** (مسارات، مفاتيح، أدوات) — البيئات تتغير بين الوكلاء. الثابت الوحيد: المستودعات الثلاثة على GitHub وقواعد المالك. كل شيء بيئي يُعاد التحقق منه بأمر فعلي قبل الاستخدام.

## ترتيب القراءة الإلزامي لأي جلسة جديدة
1. هذا الملف (BOOTSTRAP.md)
2. `STATE.md` — الحالة اللحظية: أين نحن الآن بالضبط، وما التالي
3. `MEMORY.md` — قيود المالك + الحقائق الدائمة المثبتة
4. `01-keif-aldiafa/reports/10-الخطة-المتكاملة-الشاملة.md` — الخطة الحاكمة
5. آخر ملف في `04-owner-messages/` — آخر توجيه من المالك
6. `05-incoming-research/` — هل يوجد وارد جديد لم يُحلل؟

## المستودعات الثلاثة (ثابتة)
| المستودع | الدور | القاعدة |
|---|---|---|
| MoTechSys/keif-aldiafa-web | ⭐ العمل | كل الكود هنا فقط |
| MoTechSys/keif-v2 | 🔴 الإنتاج (Vercel→keifaldiafa.com) | ⛔ لا يُلمس إلا بأمر نشر صريح من المالك |
| MoTechSys/allpro | 📚 التوثيق والذاكرة | كل قرار/تقرير/رسالة مالك يُرفع هنا فوراً |

## قواعد مضادة للهلوسة (ملزمة لكل وكيل)
1. **كل ادعاء بدليل:** ملف:سطر، أو مخرجات أمر، أو بيانات GSC/Ads، أو مصدر رسمي — وإلا لا يُكتب
2. **لا استنتاج بلا قياس:** الأداء يُقاس بأداة (Lighthouse/curl)، لا يُخمَّن
3. **التحقق قبل البناء:** أي تقرير خارجي يُتحقق من ادعاءاته (سابقة: باحث ادّعى عدم وجود GBP خطأً)
4. **اكتب فوراً، لا تؤجل:** أي حقيقة جديدة/قرار مالك → يوثق في allpro في نفس الجلسة (الذاكرة خارج المحادثة لا داخلها)
5. **حدّث STATE.md قبل نهاية كل جلسة عمل** — هو نقطة التسليم بين الجلسات
6. **عند الشك اسأل المالك** — سؤال واحد أرخص من هلوسة واحدة

## قيود المالك المقدسة (نسخة سريعة — التفصيل في MEMORY.md)
لا تسعير إطلاقاً · لا أرقام غير موثقة (proof.json) · لا ترقيع — جذري فقط · Next.js 14 App Router + SSG · عربي للمحتوى لاتيني للكود · 99% جوال — الهاتف هو الحاكم · كل قرار موثق (ماذا/لماذا/الدليل) · قبل أي دمج: tsc + lint 0 + build + guards
