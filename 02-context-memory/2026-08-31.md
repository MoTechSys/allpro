# Memory — 2026-08-31

## Active Project: كيف الضيافة Domination (keif-domination)

**Goal:** Make keifaldiafa.com #1 in Google for Jeddah hospitality/catering queries (قهوجيين، صبابين، ضيافة أعراس...) + be the source AI engines cite. Mobile-first (95% mobile). Client = real business «مؤسسة كيف الضيافة للأفراح والمناسبات», Jeddah. Verified partners: Olayan, Schneider Electric, Saudi Binladin Group, Napco, Al Ahli FC.

### Workspace layout
- Root: `/home/work/.openclaw/workspace/projects/keif-domination/`
- Context brief: `CONTEXT.md`
- Main repo (work on this): `sources/keif-aldiafa-web` — Next.js 14 App Router, 48 pages SSG, 426 images. GitHub: MoTechSys/keif-aldiafa-web
- Reference repos: `sources/royal-coffee-docs` (competitor analysis + decisions), `sources/osoul-aldiafa-v2` (seo-guard.mjs, similarity-check.mjs, golden SEO doc)
- **Golden SEO doc** (binding rules): `sources/osoul-aldiafa-v2/docs/99-archive/plan-ar/07-قواعد-السيو-الملزمة.md`
- Output reports dir: `keif-domination-report/`
- Raw data: `keif-domination-report/data/` (lh-*.json Lighthouse, serp/*.json, comp-*.json)

### Verified build state
- build ✅ 48 pages | tsc --noEmit ✅ clean | next lint ✅ 4 warnings only
- Dev/prod server ran locally on :3100
- Routes: / , /[serviceCity](24 pages!) , /about , /contact , /legal , /locations , /locations/[city](8 pages!) , /offerings , /portfolio , /services , /social

### 🔴 CRITICAL FINDINGS (all evidence-backed)
1. 24 `[serviceCity]` + 8 `locations/[city]` pages = Doorway pattern (violates N1/N2, documented −63% traffic disaster). Golden rule: ONE page per independent search INTENT, not per geography/keyword.
2. robots.ts does NOT explicitly allow GPTBot/Google-Extended/PerplexityBot/ClaudeBot
3. `CateringService` + `address` + `GeoCoordinates` schema — violates N8. We are SAB (service-area business, no physical location) → must use `ProfessionalService` + `serviceArea: GeoCircle`, NO address, NO BRANCHES, NO LocalBusiness.
4. **PERFORMANCE COLLAPSE (real Lighthouse mobile 4G):** LCP 7.7–9.2s (budget <2.5s!), TBT 670–1000ms across ALL pages. Perf score 46–58. Root causes: (a) Google Tag Manager wastes 344KB blocking main thread (gtag 186KB + AW ads 158KB, layout.tsx:201), (b) hero-bg.mp4 280KB delays LCP, (c) fonts woff2 no preload. GOOD: CLS=0, SEO=100, A11y=100. All fixable without rebuild.

### ⛔ CLIENT CONSTRAINTS (from owner, MUST respect)
- **NO PRICING PAGES** — no fixed prices, services on-demand. Focus = presenting services/specs + fast WhatsApp contact path. (Already corrected report 03: removed pricing pages → converted to "service options & specs" page steering to WhatsApp +966508252134.)
- No fake numbers ever (E-E-A-T). Proof numbers only from owner or omitted.
- No patching — root/complete solutions only.
- Arabic for content/reports, Latin for code/slugs.
- Stay on Next.js 14 App Router + SSG.

### Competitor intel (real SERP via gsk search)
- Social = 40% of results (Instagram mandatory, appears 100% in qahwajiin queries)
- kahwgysa.com = #1 competitor, wins قهوجيين/قهوجيات with ONE deep page. Weaknesses: broken 0+ counters, spelling errors, dated design, keyword stuffing footer → our openings.
- asoulaldiafa.com = our sibling site, #1 for ضيافة أعراس + ركن قهوة
- keifaldiafa.com (us) = appears in only 1 query (ضيافة أعراس, position 2)
- 💎 "أسعار قهوجيين جدة" had competitive vacuum — BUT client said no pricing, so target it via service/specs content (no numbers).

### 💎 Discovered treasure in main repo (INTEGRATE, don't duplicate)
- `sources/keif-aldiafa-web/reports/` — rich prior analyses: LIGHTHOUSE-REPORT, DEEP-ANALYSIS-SEO/UX, LOCALBUSINESS-SCHEMA-DESIGN, CRAWLABILITY-AUDIT, HOMEPAGE-UX-AUDIT.md (38KB), classified errors folder (17 categorized design errors). Confirms our findings with LIVE GSC data (indexed 9/47, the 24 city pages = doorway). These REINFORCE our reports with live evidence — merge them.
- Also good in repo: smart middleware (www→non-www 301, legacy WP redirects), next.config avif/webp + compress + security headers.

### Report status (10 deliverables → keif-domination-report/)
- ✅ 01-تدقيق-الكود.md (Codex, 18KB) — parallel city-page layers, dead `luxe` tree, SSOT violations (SITE_URL ×16, dual city sources, dual image sources)
- ✅ 02-تقرير-الأداء.md (me, real Lighthouse data)
- ✅ 03-خريطة-الكلمات-والنيات.md (Codex, corrected to remove pricing)
- ✅ 04-دراسة-منافسي-جدة.md (me)
- ✅ 07-حزمة-AEO.md (me)
- ✅ 08-منظومة-الحماية-الآلية.md (Codex, 20KB)
- 🔄 05-تشريح-الصفحة-الرئيسية.md — IN PROGRESS (was writing when compaction hit). Have golden refs: repo's HOMEPAGE-UX-AUDIT.md (line-by-line + mobile measurements) + luxury standards doc. Base is solid, integrate with my new mobile screenshots.
- ⏳ 06-جرد-الصور-والحماية.md — Agent 6 (Codex) running in background when compaction hit
- ⏳ 09-خطة-التنفيذ-المرحلية.md — not started
- ⏳ 00-التقرير-التنفيذي.md — not started (write last, synthesize all)

### FINAL DELIVERABLE OWED TO USER
User asked for a **direct download link** with ALL reports zipped. When done: zip `keif-domination-report/` and provide download link. Deliver reports progressively as each finishes.

### NEXT STEPS on resume
1. Finish writing 05 (homepage anatomy) — integrate repo's HOMEPAGE-UX-AUDIT.md + my screenshots
2. Check Agent 6 (Codex) output for report 06
3. Write 09 (phased implementation plan, ordered by impact, each phase w/ acceptance criteria)
4. Write 00 (executive summary — synthesize all, integrate live GSC data from repo reports/)
5. Zip everything + provide download link

---

## Engineering environment (from prior setup, see workspace/ENGINEERING.md)

### 5 LIVE engines (all tested working via Genspark proxy w/ PERMANENT system key)
Proxy base: `https://www.genspark.ai/api/llm_proxy/v1` (OpenAI-compatible litellm). System key in `~/.openclaw/openclaw.json → models.providers.genspark-llm-proxy.apiKey`. NOTE: short-lived `gsk-…` keys pasted in chat expire in ~30-40 min — always use permanent system key.
1. Claude opus-5 — via `sessions_spawn` ACP `claude` (policy-allowed)
2. Codex gpt-5.3-codex — via `codex exec` directly. **MUST run with `-s danger-full-access -C <dir>` and bubblewrap disabled** (bwrap loopback fails in this env). `gpt-5-codex` is 404, use `gpt-5.3-codex`.
3. GPT sol gpt-5.6-sol — proxy/opencode
4. claude-fable-5 — came back to life 2026-08-29 (was 401 upstream earlier). Registered in opencode.
5. Hermes (Nous Agent v0.11.0) — `hermes chat -Q --yolo --provider anthropic -q "..."`. Configured on genspark proxy, model claude-opus-4-8. Config: `~/.hermes/config.yaml` + `.env`. Supports `--worktree` for isolated git worktrees. NOTE: Hermes got stuck in "compacting context" on large-context tasks — prefer Codex for reliability.

### Parallelism lessons learned
- Codex is the reliable workhorse for background agent tasks (`mode:"run"` background). Ran Agents 1,3,6,8 on Codex successfully (~18-20KB quality reports each).
- Don't run two Codex branches simultaneously on same resource — they compete. One strong branch + me working in parallel = best throughput.
- Hermes unreliable on big context (compacting hang). ACP `codex` agentId NOT policy-allowed — use `codex exec` CLI directly instead.
- opencode default set to genspark-llm-proxy/claude-opus-5 (fixed from broken fable-5).

### Env quick facts
- Ubuntu 24.04, 4 vCPU, 15GB RAM, ~99GB free. Node v24.18, Python 3.12, gcc 13.
- Browser: `systemctl --user start openclaw-browser`, CDP :9222. localhost navigation BLOCKED by browser-tool policy — use `gsk screenshot --viewport ...` (returns URL, no -o flag) for screenshots instead.
- web_search tool disabled → use `gsk search` (results in `data.organic_results`).
- Screenshot binary files: NEVER `read` them — use `gsk analyze -i <path> -r <instruction>` (write output to file then read).
- 7 mandatory quality gates per project (ENGINEERING.md): typecheck, lint(0 warn), unit tests, build, e2e+a11y(axe), security(no secrets), aggregate check:all.
