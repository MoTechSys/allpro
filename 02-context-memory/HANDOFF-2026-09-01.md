# HANDOFF-2026-09-01.md — لقطة التسليم الكاملة إلى الوكيل الجديد

> **من:** الوكيل الحالي (جلسات 2026-08-31 → 2026-09-01)
> **إلى:** أي وكيل جديد يستلم المشروع على حساب/بيئة أخرى
> **أمر المالك الحرفي:** «شووف االان تجهز التوثيق والهيكليه وكل شي لاانني سووف انتقل اكمل في وكيل مثلك تماما بس بحساب اخر لذاا ارفع المعاايروالادا والقوه وكلل شي ووثق كل شي...»
> **الاستخدام:** اقرأ هذا الملف بعد `BOOTSTRAP.md` مباشرة. كل ما هنا **متحقق منه حياً** لحظة الكتابة (git rev-parse على الريموت).

---

## 1) حالة المستودعات لحظة التسليم (متحقق بـ `git fetch` + `rev-parse`)

| المستودع | الفرع | Commit | الحالة |
|---|---|---|---|
| keif-aldiafa-web | `main` | `eca4655` | = origin/main، شجرة نظيفة ✅ |
| keif-aldiafa-web | `genspark_ai_developer` | `eca4655` | = main (متطابقان) ✅ |
| keif-aldiafa-web | `old-version-2026-09-01` | `fe782e8` | فرع احتياطي ما قبل W0 — مرفوع ✅ |
| keif-v2 (الإنتاج) | — | — | ⛔ لم يُلمس — Vercel يخدم keifaldiafa.com كما هو |
| allpro | `main` | هذا الكومِت وما قبله | كل التقارير 00–17 + الذاكرة مدفوعة ✅ |

- **PR #2** (https://github.com/MoTechSys/keif-aldiafa-web/pull/2): مغلق merged=True — **آخر PR في تاريخ المشروع** (سياسة المالك: لا PRs).
- لا تغييرات غير مدموجة/غير مدفوعة في أي مستودع.

## 2) ما أُنجز حتى الآن (خلاصة الموجات)

### W0 — إصلاحات الأساس (✅ مدموجة في main @ f48edf8، تفاصيل: تقرير 16)
1. `priceRange` → `"$$-$$$$"` (حكم موثق: تقرير 13 + تفويض المالك)
2. حذف FAQPage JSON-LD من 3 صفحات + حذف `generateFAQSchema` (متوقفة رسمياً 2026-05-07 — تقرير 14؛ المحتوى المرئي باقٍ)
3. `sameAs` = 6 روابط متحققة (X/TikTok/Snapchat/Instagram/Facebook/GBP-maps؛ −wa.me؛ **يوتيوب لم يُضف — المالك لا يملك**)
4. `data/proof.json`: ادعاء 500 → `verified:true` + الصيغة المعتمدة «+500 مناسبة نفّذها فريقنا بأنفسنا في جدة والمنطقة الغربية»
5. favicon.ico أُعيد توليده بأحجام فعلية 16+32+48 + ترتيب ICO قبل SVG في layout (اكتشاف تقرير 15: SVG غير مدعومة رسمياً)

### W1-1 — أول صفحة نية (✅ مدموجة في main @ eca4655، تفاصيل: تقرير 17)
- صفحة `/mubashirin-qahwa-jeddah` («مباشرين قهوة جدة» — **405 نقرات مدفوعة كانت بلا صفحة إطلاقاً**، متحقق من بيانات Ads + 404 حي على الإنتاج ×3 صيغ slug)
- محتوى يدوي فريد: تشابه 36.3–40.3% مع صفحات جدة الشقيقة (الحد 60%، الهدف <45%) — مُقاس برمجياً
- العنوان 49 حرفاً / الوصف 153 حرفاً (محسوبان قبل الكتابة)
- سكيمات: Breadcrumb + Service + WebPage فقط · أسئلة شائعة مرئية `<details>` ×6 · لا تسعير · صيغة +500 كاملة
- **نمط `INTENT_PAGES` الجديد** في `src/lib/localPages.ts`: سجل الصفحات النوايا المستقلة خارج مصفوفة (خدمة×مدينة) — منع doorway معمارياً. يغذي تلقائياً: sitemap + بطاقات صفحة المدينة + الروابط الداخلية (`otherCitiesLinks` في `localContent.tsx`). **أي صفحة نية جديدة = سطر واحد في السجل + ملف الصفحة.**

## 3) 🎯 المهمة التالية فوراً (طابور W1 — من تقرير 11 §4)

| ترتيب | الصفحة | السبب المُقاس |
|---|---|---|
| **1 — التالي** | `/qahwajiyat-sababat-jeddah` (قهوجيات وصبابات — نسائي) | فجوة نية نسائية كاملة بلا صفحة |
| 2 | `/diyafa-a3ras-jeddah` (ضيافة أعراس) | نية مناسبات عالية القيمة |
| 3 | `/diyafa-aza-jeddah` (ضيافة عزاء) | المتصدر thawq-rafeea معطل HTTP 500 (نافذة ذهبية — أعد التحقق أنها مستمرة) |
| 4 | `/coffee-break-sharikat-jeddah` (كوفي بريك شركات B2B) | نية B2B غير مغطاة |
| بالتوازي | **معركة العناوين**: sababin-jeddah ترتيب 3.16 وCTR هزيل | أسرع مكسب — تحسين title/description فقط |

**البروتوكول لكل صفحة** = §«بروتوكول إضافة صفحة نية جديدة» في BOOTSTRAP.md (إعادة تحقق حي → INTENT_PAGES → محتوى فريد → قياس تشابه → بوابة الجودة → دمج مباشر → تقرير مرقم + STATE.md).

## 4) إعادة بناء البيئة من الصفر (الخطوات الحرفية)

```bash
# 1) استنساخ مستودع العمل (في بيئة Genspark يكون عادة مُهيأ في /home/user/webapp)
git clone https://github.com/MoTechSys/keif-aldiafa-web.git
cd keif-aldiafa-web && git checkout main   # eca4655 أو أحدث

# 2) استنساخ التوثيق
git clone https://github.com/MoTechSys/allpro.git /tmp/allpro

# 3) التبعيات والتحقق أن البيئة سليمة
npm install
npx tsc --noEmit && npx next lint && npx next build && npm run guard
# المتوقع: كله أخضر · guards: seo 121 / similarity 118 / assets 31 (خطوط الأساس)
```
- **مصادقة GitHub:** التوكن قد ينتهي منتصف الجلسة — عند فشل push أعد تهيئة بيئة GitHub (أداة `setup_github_environment` في Genspark) ثم أعد المحاولة.
- **مستنسخات المراجع** (`royal-coffee-docs`, `osoul-aldiafa-v2`) كانت في `/tmp/repos/` ببيئة سابقة — **لا تنتقل**؛ أعد استنساخها من GitHub عند الحاجة.

## 5) الديون المعروفة (لا تنساها ولا تخترع حلولاً لها)

| الدين | الحالة | الإجراء |
|---|---|---|
| ادعاءات proof.json: 200 عميل / 100% رضا / 10 سنوات | `verified:false` | لا تُستخدم في أي محتوى حتى يوثقها المالك |
| PSI API | حصة يومية مستنفدة (2026-09-01) | أعد قياس LCP الرسمي لاحقاً |
| صفحة /services | ترتيب 38.9 (GSC) | ضمن موجات لاحقة من الخطة 10 |
| similarity baseline | 118 زوجاً قديماً ≥60% (صفحات المدن doorway الموروثة) | تُعالج في موجة إعادة كتابة المدن (خيار B المحسّن — قرار موثق) |
| Preferred Sources (جوجل) | ميزة اختيارية (2026-08-20) | رابط عميق بالفوتر — **اسأل المالك قبل التنفيذ** |
| الرقم الضريبي (vatID) | المالك لا يملك حالياً | يُضاف فقط إن سجّل المالك — لا تخترع |
| صفحات الثقة /privacy /terms /faq | 404 حالياً — ضمن الخطة (~55 صفحة) | موجة لاحقة حسب ترتيب الخطة 10 |
| النشر للإنتاج | كل إنجاز W0+W1-1 غير منشور | ⛔ فقط بأمر مالك صريح، حينها تُنسق آلية النقل إلى keif-v2 |

## 6) بيانات ثابتة سريعة
- **الدومين:** keifaldiafa.com · **واتساب:** +966508252134 · **الكيان:** مؤسسة كيف الضيافة للأفراح والمناسبات · **الرقم الوطني الموحد:** 7033069720 · لا رقم ضريبي
- **schema الأساس:** CateringService + provider graph · priceRange `"$$-$$$$"` · sameAs ×6 (بلا يوتيوب، بلا wa.me)
- **حدود الميتا:** title ≤60 حرفاً شاملاً « | كيف الضيافة» · description ≤158
- **الجمهور:** 95%+ جوال (390px هو الحاكم)

## 7) رفع المعايير — ما يُتوقع من الوكيل الجديد (أمر المالك: «ارفع المعايير والأداء والقوة»)
1. **هدف التشابه للصفحات الجديدة: <45%** (لا الاكتفاء بـ<60%) — آخر صفحة حققت 36–40%، حافظ على هذا المستوى أو أفضل
2. **قياس ما بعد النشر:** بعد أول نشر إنتاجي، فعّل دورة GSC أسبوعية (فهرسة الصفحات الجديدة + CTR معركة العناوين) ووثقها بتقارير مرقمة
3. **صفر مخالفات جديدة في الحراس** ليست سقفاً بل أرضية — كلما لمست ملفاً قديماً أصلح مخالفاته القديمة (النمط: W0 أنقصت seo-guard من 156 إلى 121)
4. **كل جلسة تنتهي بـ:** push للمستودعين + تحديث STATE.md — بلا استثناء
5. **التوثيق بجودة تسليم دائمة:** اكتب كل تقرير وكأن وكيلاً جديداً سيستلم غداً

_كُتب وتُحقق منه حياً: 2026-09-01 — جلسة التسليم الأخيرة للوكيل الحالي_
