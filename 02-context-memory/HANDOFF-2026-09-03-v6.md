# HANDOFF — 2026-09-03 — نموذج v6 (كل صفحات الموقع) — نقطة استكمال الوكيل الجديد

> **اقرأ هذا الملف أولاً.** كُتب بأمر المالك: «وثق كل شي في المستودع كي نكمل في جلسه اخرئ بحيث الوكيل الاخر يكون فاهم كلل شي ويكمل بدقه وقوه — الان فورا لا تشتغل ابدا ووثق بس بحيث نكمل في حسااب جديد».
> ترتيب القراءة بعده: `02-context-memory/STATE.md` (آخر 3 قيود) → `prototype-home/README.md` §v6 → `01-keif-aldiafa/reports/25-*.md` → `04-owner-messages/` (آخر 3 ملفات) → ثم `BOOTSTRAP.md`/`IDENTITY.md`/`USER.md`/`ENGINEERING.md` للسياق العام.

---

## 0. من هو المالك وكيف يتعامل
- المالك: **MoTechSys** — يُنادَى «يا غالي». صاحب «كيف الضيافة» keifaldiafa.com (ضيافة فاخرة، جدة + مدن المملكة).
- يكتب بالعامية وباختزال («استمررر بقووه»). **فسّر النيّة لا الحرف.** يفوّض بالكامل («لا تنتظر مني أي قرار حلل ودقق وسوي كل شي بأفضل ما يمكن»).
- يريد: تقارير عربية موجزة بروابط، توثيق كل قرار، حفظ رسائله **حرفياً** في `04-owner-messages/`.

## 1. السياسات الثابتة (لا تُخالَف)
| # | السياسة |
|---|---|
| P1 | **لا PRs** — commit + push مباشر إلى `main` في `MoTechSys/allpro`، ثم مزامنة مستودع `MoTechSys/khadamat`. |
| P2 | **لا أسعار** في أي صفحة (لا ريال/SAR/ر.س). |
| P3 | واتساب واحد: `966508252134` (عرض `0508252134`). الأرقام المسموحة فقط: **+500 مناسبة**، **منذ 2016**، سجل تجاري **7033069720**. |
| P4 | **لا اسم/شعار جهة حكومية** (أمانة، وزارة، هيئة، جامعة، غرفة، منتدى…) بلا موافقة كتابية. شعارات **الشركات مسموحة** وتُوسَم «شركة». |
| P5 | لا YouTube. |
| P6 | **الإنتاج `keif-aldiafa-web/` (keif-v2) لا يُمسّ** — مصدر صور فقط. |
| P7 | اختبار دائم على **390 (جوال) و1440 (ديسكتوب)**: 0 أخطاء console، 0 تمرير أفقي، Lighthouse جوال ≥90، إتاحة 100. |
| P8 | كل خطوة تُقيَّد في `STATE.md` وتُرفَع. |
| P9 | النموذج `noindex` (SEO 63 في Lighthouse مقصود). |

## 2. أين نحن الآن (الحالة الفعلية لحظة التوقّف)
**v6 مبني بالكامل ويعمل محلياً؛ مُوثَّق ومرفوع؛ يتبقّى تحسين أداء صفحتين + لمسات بصرية + تقرير للمالك.**

### 2.1 ما أُنجز في v6 (هذه الجلسة)
1. **مولّد صفحات** `prototype-home/build/build.py` يقتطع الغلاف المشترك (CSS، خطوط، header، drawer، footer، fab، lightbox، script) **حرفياً من `index.html`** ويركّب 5 صفحات: `services.html`, `offerings.html`, `portfolio.html`, `about.html`, `contact.html`. → مصدر الحقيقة الوحيد للغلاف هو `index.html`؛ **أي تعديل على الغلاف يكون في index.html ثم `python3 build.py`**.
2. **بيانات المحتوى** `build/data.py` (15 خدمة في 4 مجموعات، 9 فئات تقديمات/76 صنفاً + 28 معدة + 5 توزيعات، تعليقات الأعمال، من نحن، الأسئلة).
3. **خطّ صور** `build/images.py` (PIL): 231 صورة جديدة من `keif-aldiafa-web/public/images` → `img/photos/` (≤640px q76) + `img/full/` (≤1600px q82) + `build/images-manifest.json` {map, sizes}. **يرفض تلقائياً** أي صورة حكمها GOV/CAUTION في `build/photo-vetting.json` (SystemExit).
4. **تدقيق صور** `build/photo-vetting.json`: 51 صورة (COMPANY 19 · GOV 15 · SAFE 12 · FLAG 3 · CAUTION 2). GOV+CAUTION مستبعدة كلياً.
5. **الرئيسية** `index.html` أُعيد توصيلها محلياً (كل الروابط → الصفحات الجديدة؛ الخارجي الوحيد `/legal`)، 4 وسوم صُحِّحت («جهة حكومية» → «شركة/فعالية رسمية» حيث لا يوجد جهة)، القائمة الجانبية فيها مجموعة «الصفحات»، خطافات `data-ev` + `window.dataLayer` (`cta_click`، `lead_wa`)، الشريط «v6 · 2026-09-03».
6. **إصلاحات أثناء التشغيل الأول**: (أ) `sv-zamzam` الفهرس 2..8 لا 1..7 (data.py سطر 25) · (ب) كتلة الـfab الموروثة كانت تُخفي زر واتساب في الصفحات الفرعية → `if(fab && hero)` في build.py (PAGE_JS يتولّى الـfab بالـ`.phero`) · (ج) صور المشروبات الحارة صور صواني حقيقية تظهر «سترة النادل» في القصّ المربّع → `#hot .item img{object-position:50% 78%}`.

### 2.2 نتائج التدقيق (حقيقية، ليست تقديرية)
**Playwright (390 + 1440) — كل الصفحات الست:** 0 أخطاء console · 0 تمرير أفقي · الروابط العميقة تعمل (`services.html#zamzam` يمرّر ويُضيء `.hit`؛ `offerings.html#equipment` كذلك) · الـLightbox يفتح النسخة الكاملة `img/full/sv-hosts-1.webp` بالتعليق الصحيح · فلاتر الأعمال: `?type=government` → «4 · جهات حكومية ورسمية» والتنويه يظهر؛ `?type=weddings` (اسم الإنتاج) → alias → 12 صورة، التنويه مخفي · نموذج التواصل: `?service=zamzam` يملأ القائمة؛ الإرسال يفتح `wa.me/966508252134` برسالة منسّقة ويدفع `lead_wa` إلى dataLayer · الـfab يظهر بعد التمرير في الصفحات الفرعية.
**فحص ثابت (`/tmp/check.py` — أعِد كتابته إن لزم):** 0 صور مفقودة · 0 مراسي مكسورة (كل `href="x.html#id"` له `id`) · 0 أسعار · 0 أرقام واتساب غير الرقم الرسمي · 0 معرّفات مكرّرة · كلمات «وزارة/هيئة» تظهر **فقط** في `alt` شارتي وزارة التجارة وهيئة الزكاة (شارات رسمية موجودة أصلاً في الإنتاج — مسموحة).

**Lighthouse 12 جوال (Chromium، محاكاة خنق):**
| الصفحة | أداء | إتاحة | أفضل ممارسات | SEO | LCP |
|---|---|---|---|---|---|
| index (v5.3 مرجع) | 92 | 100 | 100 | 63 | 2.2s |
| services | **81** | 100 | 96 | 63 | 2.2s |
| offerings | **71** | 100 | 96 | 63 | 4.9s |
| portfolio | **69** | 100 | 96 | 63 | 5.9s |
| about | 99 | 100 | 96 | 63 | 1.7s |
| contact | 98 | 100 | 96 | 63 | 2.2s |

- **SEO 63 مقصود** (`noindex` للنموذج).
- **أفضل ممارسات 96** سببه `image-aspect-ratio` على `img/logo-emblem.webp` (يُعرض 226×44 وأصله 226×271 — أي أن CSS الشعار في الهيدر يمطّه/يقصّه). **سهل الإصلاح** (انظر §3 مهمة 2).
- **الأداء 69–81 في 3 صفحات**: السبب الرئيس أن **صورة هيرو الصفحة (`.phero img.bg`) هي مصغّرة 480×640/640×640 تُمَدّ على 100vw** (`uses-responsive-images` يقدّر توفير 456–489 KiB) + في portfolio/services عدد كبير من الصور. **الحلّ معروف** (§3 مهمة 1).

### 2.3 لقطات الشاشة
`01-keif-aldiafa/reports/shots/v6/{m,d}-{index,services,offerings,portfolio,about,contact}.png` (m=390، d=1440). مراجعتها بصرياً أظهرت: الغلاف موحّد، الشرائح (chips) تعمل، البطاقات صحيحة. لم يُكتشف عيب تخطيطي سوى قصّ صور المشروبات الحارة (أُصلح).

## 3. المهام المتبقية بالترتيب (للوكيل الجديد)
1. **أداء offerings/portfolio/services إلى ≥90 جوال** — الحل: توليد نسخة هيرو مخصّصة لكل صفحة من `img/full/<hero>.webp` بمقاسات 800/1200/1600 (كما فعلنا في v5.3 لـindex: `<picture>` + `srcset` + `sizes=100vw` + `preload` مشروط)، في `images.py` أضف دالة `hero_variants()` وفي `build.py` عدّل `phero()` لتخرج `<picture>`. صور الهيرو الحالية: services=`s-hosts`, offerings=`o-dallah`, portfolio=`p-gala`, about/contact=hero الرئيسية. تحقّق أيضاً `dom-size` في offerings (914 عنصر — مقبول لكن يمكن إخفاء أصناف الفئة بعد 8 خلف «عرض الكل»).
2. **أفضل ممارسات 100**: شعار الهيدر `img/logo-emblem.webp` (226×271) يُعرض بارتفاع 44 — إمّا تصحيح CSS `.brand img{height:44px;width:auto}` أو تصدير نسخة بأبعاد العرض الحقيقية. **التعديل في `index.html` ثم `build.py`** (يسري على الكل).
3. **مراجعة بصرية كاملة الطول** (full_page) للصفحات الخمس على 390 و1440 وليس أعلى الصفحة فقط — وحفظ اللقطات في `reports/shots/v6/`.
4. **تقرير للمالك بالعربية** (موجز + روابط + ما يحتاج قراره) — القرارات المعلّقة عليه في §5.
5. اختياري: **نشر مستضاف** فقط عند طلب المالك. تفعيل GitHub Pages لـkhadamat يدوياً (API أعطى 403).
6. **للإنتاج لاحقاً (keif-v2)**: يجب أن تدعم صفحاته المراسي `services#<id>`, `offerings#<id>`, `portfolio?type=government|corporate|private|equipment` (+ الأسماء القديمة `events→corporate`, `weddings→private`), و`contact?service=<id>`. حالياً صفحات الإنتاج تبويبات JS بلا مراسي.

## 4. كيف تعمل (أوامر جاهزة)
```bash
cd /home/user/webapp                                   # مستودع allpro (branch main)
cd prototype-home && python3 -m http.server 8787 &     # المعاينة: /index.html /services.html ...
cd build && python3 images.py                          # (فقط إن أضفت صوراً) — يقرأ keif-aldiafa-web/public/images
python3 build.py                                       # يولّد الصفحات الخمس من index.html + data.py
```
- الغلاف (CSS/هيدر/تذييل/Lightbox/JS) **يُعدَّل في `index.html` فقط**؛ محتوى الصفحات في `data.py`؛ هيكل الصفحات/CSS الخاص بها (`PAGE_CSS`/`PAGE_JS`) في `build.py`.
- **معرّفات المراسي** (يجب أن تبقى ثابتة لأن index يشير إليها): الخدمات `hosts zamzam safarjia sawas hostesses safarjiat cleaning-female calligrapher artist folkband heritage-tent counter photo-booth buffet mobile-table` · التقديمات `hot cold dates sweets pastry snacks sandwiches fruits nuts equipment distributions` · تواصل `#cities`.
- Lighthouse: `/tmp/lh` قد لا يكون موجوداً في حساب جديد → `npm i lighthouse` في مجلد مؤقت، `CHROME_PATH` = Chromium الخاص بـPlaywright (`~/.cache/ms-playwright/chromium-*/chrome-linux*/chrome`) مع `--chrome-flags="--headless=new --no-sandbox"`.
- Playwright: `pip install playwright && playwright install chromium`. قالب التدقيق موصوف في تقرير 25 §5 (أعد كتابته).
- **مزامنة khadamat**: `git clone https://github.com/MoTechSys/khadamat /tmp/khadamat && rm -rf /tmp/khadamat/* && cp -r prototype-home/* /tmp/khadamat/ && cd /tmp/khadamat && git add -A && git commit -m "sync v6" && git push` (يحتاج بيانات دخول GitHub للمالك — استخدم `setup_github_environment`).

## 5. قرارات اتُّخذت نيابةً عن المالك (تحتاج تأكيده لاحقاً — ليست عائقاً)
| القرار | البديل إن رفض |
|---|---|
| صور المكسرات كلها بعلامة **NOIX** → أُبقيت بأسماء صادقة (`NUTS_FIX`) + ملاحظة «منتجات شركات شريكة» (سياسة P4 تسمح بالشركات) | استبدالها بصور مكسرات عامة أو تصوير جديد |
| 4 صور «جهات حكومية» في الأعمال = SAFE/FLAG فقط (علم/أجواء رسمية بلا اسم) | إن أعطى موافقات كتابية → تُفتح 15 صورة GOV المستبعدة |
| صورتا CAUTION (`table-5`, `tent-3`) مستبعدتان احتياطاً | يمكن إعادتهما إن حسمهما |
| الخدمات 15 لا 12 (أُضيف `artist`, `cleaning-female`, `mobile-table` من الإنتاج) | حذف أي منها من `SERVICE_GROUPS` |
| نموذج التواصل **بلا خادم** (يبني رسالة واتساب) | ربطه لاحقاً بـEmail/CRM |
| تقييم Google 4.5★ / 49 مراجعة معروض في «من نحن» (من بيانات الإنتاج) | يُحدَّث دورياً أو يُحذف |

## 6. المستودعات والروابط
- **allpro** (هذا): `https://github.com/MoTechSys/allpro` — branch `main`. آخر commit قبل هذه الجلسة `bc0a7a7`؛ commit هذه الجلسة **`7045ac2`** (+ commit التوثيق النهائي بعده).
- **khadamat** (نسخة النموذج للمالك): `https://github.com/MoTechSys/khadamat` — **مُزامَن مع v6: commit `fa323ec`** (كان `440d4e8` v5.3).
- روابط sandbox القديمة (8787/8788/8790) **تموت مع الحساب القديم** — أعد تشغيل الخادم.
- الإنتاج: https://keifaldiafa.com (لا يُمسّ).

## 7. خريطة الملفات المهمة
```
prototype-home/
  index.html                 # الرئيسية v6 — مصدر الغلاف المشترك (لا تعدّل الغلاف في الصفحات المولّدة!)
  services.html offerings.html portfolio.html about.html contact.html   # مولَّدة — لا تُحرَّر يدوياً
  build/build.py             # المولّد (cut() للغلاف + PAGE_CSS/PAGE_JS + build_*())
  build/data.py              # المحتوى (خدمات/تقديمات/أعمال/من نحن/أسئلة/تصحيحات أسماء)
  build/images.py            # خطّ الصور + BAN من photo-vetting.json
  build/images-manifest.json # اسم الصورة → مصدرها + أبعادها
  build/photo-vetting.json   # 51 حكماً + policy_notes
  build/prod-data.json       # جرد الإنتاج (تقديمات 9/76، خدمات جزئي — data.py هو المعتمد)
  img/photos/ (286) img/full/ (274) video/hero-bg.mp4
  README.md                  # §v6 في الأعلى
01-keif-aldiafa/reports/25-*.md      # تقرير v6 + جدول التدقيق + القرارات
01-keif-aldiafa/reports/shots/v6/    # 12 لقطة
02-context-memory/STATE.md           # سجل زمني — أضف قيداً لكل جلسة
04-owner-messages/                   # رسائل المالك حرفياً
keif-aldiafa-web/                    # الإنتاج — قراءة فقط
```
