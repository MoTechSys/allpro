# تقرير تدقيق معماري عميق — مستودع Next.js 14 (`keif-aldiafa-web`)

## نطاق الفحص ومنهجيته

- تم فحص ملفات `src/` فعلياً (قراءة مباشرة للملفات) مع تدقيق خاص للملفات المطلوبة:
  - `src/app/[serviceCity]/page.tsx`
  - `src/lib/localPages.ts`
  - `src/lib/cities.ts`
  - `src/app/locations/[city]/page.tsx`
  - `src/lib/schema.ts`
  - مكوّنات الصور في `src/components`
- تم تشغيل فحص `eslint` عبر `npm run lint` لاستخراج مخالفات React/Next.js الفعلية.
- تم توثيق كل ملاحظة بمسار وموضع سطر صريح.

---

## 1) تشخيص شامل لملفات `src/` (الأنماط العامة)

### أ) مؤشرات «ترقيع كود فوق كود»

1. **طبقتان متوازيتان لصفحات محلية (قديم/جديد) مع بقايا غير مستخدمة**
   - `src/lib/localContent.tsx:56` يبني صوراً بآلية `pickImages`.
   - `src/lib/localContent.tsx:264` و `src/lib/localContent.tsx:266` و `src/lib/localContent.tsx:274` يمرّر `heroImage/sections/gallery` داخل props.
   - `src/components/LocalServicePage.tsx:145` يولّد `art` من `getPageImagery` ويستخدمه مباشرة بدلاً من الصور القادمة من `localContent`.
   - `src/components/LocalServicePage.tsx:165` يستخدم `art.hero.src` وليس `props.heroImage`.
   - النتيجة: مصدر صور مزدوج + بيانات مبنية ولا تُستخدم فعلياً (ترقيع فوق سلوك سابق).

2. **شجرة Luxe كاملة باقية لكن غير موصولة بالتطبيق**
   - مكوّنات `luxe` موجودة (`src/components/luxe/LuxeServicePage.tsx:92`, `src/components/luxe/LuxeReveal.tsx:16`, `src/components/luxe/LuxeRequest.tsx:38`) لكنها غير مستوردة من مسارات التطبيق الحالية.
   - هذه كتلة كبيرة من كود ميت تزيد كلفة الصيانة والتشويش المعماري.

### ب) تكرار وخرق Single Source of Truth

1. **تكرار ثابت الدومين**
   - نفس الثابت `SITE_URL` مكرر في 16 موضعاً، أمثلة: `src/app/layout.tsx:15`, `src/components/SEO.tsx:3`, `src/app/[serviceCity]/page.tsx:14`, `src/app/locations/[city]/page.tsx:18`.

2. **تكرار بيانات التواصل (واتساب/هاتف)**
   - أرقام التواصل مكررة في طبقات متعددة: `src/components/Navbar.tsx:9`, `src/components/LocalServicePage.tsx:65`, `src/app/contact/ContactClient.tsx:7`, `src/lib/schema.ts:5`, `src/app/social/SocialClient.tsx:49`.

3. **مصدران مستقلان للمدن (تناقض SSOT)**
   - `src/lib/localPages.ts:20` يعرّف `CITIES` (مفاتيح إنجليزية لصفحات الخدمة×مدينة).
   - `src/lib/cities.ts:28` يعرّف `CITIES` أخرى (slugs عربية لصفحات locations).
   - الربط بين المصدرين يتم بالمطابقة النصية لاسم المدينة: `src/app/locations/[city]/page.tsx:225`.
   - هذا ربط هش: أي تعديل تحريري في الاسم يكسر روابط الخدمة×المدينة دون حماية نوعية.

4. **مصدران مستقلان للصور والمخزون**
   - `src/lib/imageCatalog.ts:4` يصرّح أن مصدر الحقيقة هو فحص `public/images`.
   - في المقابل `src/lib/images.ts:1` يحافظ على فهارس يدوية ضخمة للصور.
   - الاستهلاك الفعلي موزّع بين المصدرين (مثلاً `ServicesClient` من `lib/images` في `src/app/services/ServicesClient.tsx:9`، بينما `services/page.tsx` يعتمد `getImagesForPage` من `imageCatalog` في `src/app/services/page.tsx:10`).

### ج) كود ميت/غير مستخدم

1. `src/components/OptimizedImage.tsx:70` مكوّن مصدّر بلا استهلاك داخل `src`.
2. `src/components/ImageSkeleton.tsx:10` و `src/components/ImageSkeleton.tsx:38` (المكوّن والهوك) بلا استهلاك داخل `src`.
3. `src/lib/utils.ts:4` (`cn`) بلا استهلاك داخل `src`.
4. `src/components/SEO.tsx:125` (`SEO_CONSTANTS`) مصدّر وغير مستخدم.
5. شجرة Luxe (مذكورة أعلاه) غير مستهلكة تطبيقياً.

### د) أخطاء/انتهاكات React/Next.js (مؤكدة بفحص lint)

1. حقن Google Analytics كسكربت inline في `layout`:
   - `src/app/layout.tsx:201` و `src/app/layout.tsx:205` (تحذير `@next/next/next-script-for-ga`).

2. استخدام `<img>` بدل `next/image`:
   - `src/components/DallahLogo.tsx:14` (تحذير `@next/next/no-img-element`).

3. تحذيرات alt في `ImageWithFallback`:
   - تحذيران في `src/components/ImageWithFallback.tsx:88` و `src/components/ImageWithFallback.tsx:92` بسبب تمرير props بشكل يربك قاعدة a11y.

4. **تداخل معالم `<main>` (Landmark invalid nesting)**
   - `src/components/ClientLayout.tsx:41` يلف جميع الصفحات داخل `<main>`.
   - صفحات/عملاء عدة تعيد `<main>` داخلياً مثل `src/app/locations/page.tsx:33`, `src/app/locations/[city]/page.tsx:104`, `src/app/legal/page.tsx:16`, `src/app/social/SocialClient.tsx:113`, `src/app/offerings/OfferingsClient.tsx:440`.
   - النتيجة: بنية دلالية غير سليمة للوصولية ومحركات القراءة.

---

## 2) تحليل الصفحات الـ24 (`service × city`)

## كيف تُولّد الصفحات؟

- `src/lib/localPages.ts:89` يعرّف 3 خدمات.
- `src/lib/localPages.ts:20` يعرّف 8 مدن.
- `src/lib/localPages.ts:114`–`src/lib/localPages.ts:116` يبني حاصل الضرب الديكارتي مباشرة (`flatMap`) ⇒ **24 صفحة**.
- `src/app/[serviceCity]/page.tsx:27` يخرج جميع slugs عبر `generateStaticParams`.

## هل هي Doorway/template متكرر؟

**النتيجة: نعم، الخطر مرتفع (Doorway-like).**

الأسباب الملموسة في الكود:

1. **هيكل المحتوى يعتمد على 3 قوالب خدمة فقط**
   - العنوان H1 يتبدل حسب نوع الخدمة فقط (`isMunasabat`/`isQahwajiin`/else): `src/lib/localContent.tsx:64`–`src/lib/localContent.tsx:68`.
   - الأقسام النصية الثلاثة تُبنى بقوالب ثابتة لكل خدمة مع إدراج اسم المدينة فقط: `src/lib/localContent.tsx:100`–`src/lib/localContent.tsx:161`.
   - «الخدمات/الباقات» بنفس النمط لكل خدمة: `src/lib/localContent.tsx:172`–`src/lib/localContent.tsx:224`.
   - FAQ أيضاً متماثلة لكل خدمة مع استبدال `${c.ar}`: `src/lib/localContent.tsx:235`–`src/lib/localContent.tsx:257`.

2. **طبقة العرض واحدة لجميع الـ24 صفحة**
   - جميع الصفحات تسقط في نفس المكوّن `LocalServicePage`: `src/app/[serviceCity]/page.tsx:97`.

3. **الاختلاف الحقيقي بين المدن محدود**
   - أكبر الفروق تأتي من `c.intro` و`districts` من `localPages` (`src/lib/localContent.tsx:95`–`src/lib/localContent.tsx:98` و `src/lib/localContent.tsx:267`)، بينما الهيكل والغالبية العظمى من الجمل تتكرر قالبياً.

**استنتاج معماري:**
النظام الحالي أقرب إلى «مصفوفة SEO صفحات مال» من صفحات نية مستخدم مستقلة، وهذا نمط Doorway تقنياً حتى لو كان النص العربي طويلاً.

---

## 3) تحليل صفحات `locations/[city]` (الـ8 صفحات)

## كيف تُولّد؟

- `src/lib/cities.ts:28` يحتوي مصفوفة المدن.
- `src/app/locations/[city]/page.tsx:25` يولّد `generateStaticParams` من `CITIES.map` ⇒ **8 صفحات**.

## هل هي Doorway أيضاً؟

**النتيجة: المخاطر أقل من الـ24، لكنها ما تزال Template ثقيلة.**

1. **الهيكل البرمجي ثابت بالكامل**
   - نفس Hero/Sections/Gallery/FAQ/CTA وروابط داخلية في كل الصفحات: `src/app/locations/[city]/page.tsx:104`–`src/app/locations/[city]/page.tsx:257`.

2. **لكن توجد بيانات مدينة فريدة نسبياً**
   - لكل مدينة intro/body/highlights/faqs/keywords مخصصة داخل `cities.ts` (أمثلة: `src/lib/cities.ts:33`, `src/lib/cities.ts:60`, `src/lib/cities.ts:87`, `src/lib/cities.ts:109`, `src/lib/cities.ts:130`, `src/lib/cities.ts:151`, `src/lib/cities.ts:169`, `src/lib/cities.ts:186`).

3. **هشاشة الربط مع نظام الخدمة×مدينة**
   - الربط يتم بالاسم العربي النصي (`SC_CITIES[k].ar === city.name`) في `src/app/locations/[city]/page.tsx:225`، وهو اقتران fragile بين مصدرين بيانات منفصلين.

**استنتاج معماري:**
ليست Doorway بنفس حدّة الـ24 صفحة، لكنها تعتمد قالباً موحداً بشدة؛ ويجب تحويلها إلى «نماذج مدينة» مبنية على بيانات موحدة بكيانات IDs لا مطابقة نصية.

---

## 4) تدقيق `schema.ts` مقابل المطلوب

المطلوب: **`ProfessionalService` بلا `address`**.

الحالة الحالية:

1. المولد الرئيسي يستخدم `CateringService` وليس `ProfessionalService`:
   - `src/lib/schema.ts:62`.

2. يوجد `address` و`geo` صريحان:
   - `src/lib/schema.ts:75`–`src/lib/schema.ts:85`.

3. `generateServiceSchema` يعرّف المزود كـ `LocalBusiness`:
   - `src/lib/schema.ts:239`–`src/lib/schema.ts:242`.

4. **لا يوجد حقل BRANCHES وهمي مباشر**
   - لا توجد مصفوفة `branch/branches` داخل `schema.ts`.
   - الموجود هو `areaServed/serviceArea` على مستوى المدن: `src/lib/schema.ts:49`–`src/lib/schema.ts:58` و `src/lib/schema.ts:86`–`src/lib/schema.ts:87`.

**الخلاصة:** لا يوجد BRANCHES صريحة، لكن النوع والحقول لا تطابق شرطك (ينبغي ProfessionalService بدون address).

---

## 5) مكونات الصور في `src/components`

## هل `OptimizedImage` / `ImageWithFallback` / `ProtectedImage` تكرار؟

**النتيجة: نعم، يوجد تكرار وظيفي وتشتت مسؤولية.**

1. **تكرار fallback نفسه**
   - نفس `ERROR_IMG_SRC` موجود في:
     - `src/components/OptimizedImage.tsx:6`
     - `src/components/ImageWithFallback.tsx:6`

2. **تكرار shimmer placeholder بصيغتين مختلفتين**
   - `shimmerBase64` في `OptimizedImage`: `src/components/OptimizedImage.tsx:12`.
   - `shimmerBase64` آخر في `ImageWithFallback`: `src/components/ImageWithFallback.tsx:10`.

3. **تداخل مسؤوليات حول `next/image`**
   - `ImageWithFallback` و`OptimizedImage` يحلان نفس المشكلة الجوهرية (تحميل + fallback + blur).
   - `ProtectedImage` يضيف حماية/علامة مائية لكنه أيضاً يلف `next/image` (`src/components/ProtectedImage.tsx:60`).

4. **إشارة عملية إلى خلل التصميم**
   - `OptimizedImage` غير مستخدم (كود ميت) بينما `ImageWithFallback` و`ProtectedImage` مستخدمان؛ هذا يوحي بمحاولات متتالية بدل واجهة موحدة واضحة.

**التوصية الجذرية:** بناء `BaseImage` واحد (Server-safe + alt strict + fallback) مع إضافات composable (`withWatermark`, `withProtection`) بدل ثلاث واجهات متداخلة.

---

## 6) جدول العيوب (ملف/سطر/العيب/التصنيف/الحل الجذري)

| الملف/السطر | العيب | التصنيف | الحل الجذري (لا ترقيع) |
|---|---|---|---|
| `src/lib/localPages.ts:114` | توليد صفحات الخدمة×مدينة بحاصل ضرب مباشر بدون نمذجة نية مستقلة | P0 | استبدال مصفوفة الضرب بنموذج `LandingIntent` صريح: كل صفحة لها intent/copy blocks/proof/data مستقل؛ إبقاء التوليد الآلي للروابط فقط لا للمحتوى. |
| `src/lib/localContent.tsx:100` | المحتوى مبني على 3 قوالب خدمة مع استبدال اسم المدينة؛ تشابه مرتفع (Doorway-like) | P0 | إعادة تصميم مولد المحتوى ليأخذ `content modules` لكل مدينة+خدمة (proof, offers, local entities, testimonials) مع قياس تشابه آلي قبل النشر. |
| `src/app/[serviceCity]/page.tsx:97` | كل الـ24 صفحة تُعرض بمكوّن واحد بنفس البنية، ما يضاعف نمط التماثل | P1 | دعم قوالب متعددة بحسب intent class (wedding, corporate, majlis...) بدل قالب واحد عالمي. |
| `src/lib/schema.ts:62` | استخدام `CateringService` بدلاً من `ProfessionalService` خلاف المتطلب | P0 | توحيد مولد النشاط إلى `generateProfessionalServiceSchema()`، واستخدام نفس `@id` في جميع schemas التابعة. |
| `src/lib/schema.ts:75` | وجود `address`/`geo` رغم شرط «ProfessionalService بلا address» | P0 | إزالة `address` و`geo` من كيان النشاط الأساسي، والاكتفاء بـ `areaServed` + `contactPoint` + `sameAs` وفق نموذج SAB المطلوب. |
| `src/lib/schema.ts:239` | `generateServiceSchema` يعرّف provider كـ `LocalBusiness` بينما الكيان الأساسي مختلف | P1 | جعل provider مرجع `@id` فقط للكيان الأساسي الموحد، بلا إعادة تعريف نوع مختلفة داخل كل Service schema. |
| `src/lib/localPages.ts:20` + `src/lib/cities.ts:28` | مصدران مستقلان للمدن (خرق SSOT) | P1 | إنشاء `src/lib/geo.ts` كمصدر مدن موحّد (ids/slugs/ar/region/districts/seo blocks)، وتوليد مشتقات locations/service-city منه. |
| `src/app/locations/[city]/page.tsx:225` | الربط بين نظامين عبر مطابقة نص الاسم العربي (fragile coupling) | P1 | الربط عبر `cityId` ثابت ومشترك بين النظامين، ومنع أي mapping نصي بالاسم. |
| `src/components/LocalServicePage.tsx:77` | حقول props (`heroImage`, `heroAlt`, `pricingNote`) معرفة لكن غير مستخدمة فعلياً | P2 | تنظيف عقد `LocalServicePageProps` ليعكس الاستخدام الحقيقي، أو إعادة استخدامها فعلياً وإزالة `getPageImagery` الموازي. |
| `src/lib/localContent.tsx:264` + `src/components/LocalServicePage.tsx:145` | بناء بيانات صور في `localContent` ثم تجاهلها في `LocalServicePage` (ترقيع فوق ترقيع) | P1 | اختيار مسار واحد فقط للصور: إمّا data-driven من `localContent` أو visual-driven من `localImagery`، وإلغاء الآخر بالكامل. |
| `src/components/OptimizedImage.tsx:70` | مكوّن صور كامل غير مستخدم (كود ميت) | P2 | حذفه إن لم يعد مطلوباً، أو دمجه في طبقة صور موحدة واستبدال جميع الاستعمالات تدريجياً. |
| `src/components/ImageSkeleton.tsx:10` | مكوّن + hook غير مستخدمين | P2 | حذف الملف أو إدخاله فعلياً في سياسة تحميل صور موحدة؛ منع بقاء ملفات utility غير مربوطة. |
| `src/lib/utils.ts:4` | `cn` غير مستخدمة | P2 | إما اعتماد `cn` عبر المشروع لتوحيد class merging أو حذف الملف لتقليل الضجيج. |
| `src/components/luxe/LuxeServicePage.tsx:92` | شجرة Luxe (وملفاتها) غير موصولة (dead subtree) | P2 | إما إعادة إدماجها رسمياً في routing/design system أو إزالتها مع أرشفة قرار التصميم. |
| `src/app/layout.tsx:201` | سكربت GA inline بدل `next/script` (تحذير Next) | P1 | نقل GA bootstrap إلى `next/script` وفق نمط Next الرسمي، وفصل JSON-LD عن سكربتات التتبع. |
| `src/components/DallahLogo.tsx:14` | استخدام `<img>` بدل `next/image` | P2 | تحويل الشعار إلى `next/image` مع `width/height` صريحين و`priority` انتقائي حسب موضع الشعار. |
| `src/components/ImageWithFallback.tsx:88` | تحذيرات alt/a11y بسبب بنية props spread | P2 | جعل `alt` prop إلزامي Explicit على عنصر `Image` دون spread غامض، واستخراج نوع `StrictImageProps`. |
| `src/components/ClientLayout.tsx:41` | `<main>` عام يسبب تداخل `<main>` داخل صفحات/عملاء آخرين | P1 | إزالة `<main>` من `ClientLayout` واستبداله بـ`<div id="main-content">` مع ترك `<main>` لكل صفحة فقط. |
| `src/components/SEO.tsx:3` + `src/app/layout.tsx:15` | تكرار ثابتات الموقع عبر طبقات كثيرة | P2 | إنشاء `src/lib/site-config.ts` يحوي `siteUrl`, `brand`, `phones`, `socials` ويُستورد وحيداً في metadata/schema/ui. |

---

## 7) ملاحظات ختامية تنفيذية

- أخطر محورين حالياً: **(1) بنية صفحات الـ24 doorway-like** و **(2) schema غير مطابق لمتطلب ProfessionalService بلا address**.
- أكبر خرق SSOT: **المدن + الصور + الثوابت** موزعة على مصادر متوازية.
- قبل أي تحسين شكلي، الأولوية المعمارية:
  1) توحيد مصادر البيانات (`geo`, `site-config`, `image-source`).
  2) إعادة نمذجة local landing pages حسب intent حقيقي وليس matrix فقط.
  3) توحيد طبقة الصور في مكوّن أساس واحد.

