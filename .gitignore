# secrets — never commit
*.token
*.key
*.pem
.env
.env.*
**/secrets*
**/credentials.json
*password*
*secret*

# noise
.DS_Store
node_modules/
*.log
Thumbs.db

# مستودع الكود المستنسخ محلياً (مستودع git مستقل — لا يُدفع ضمن allpro)
keif-aldiafa-web/
prototype-home/review/*.zip
